/*
 * Game.h
 *
 * Tracks the state of the game, the game state must be robust to incomplete
 * information, so that if the user's state doesnt know where something is (and
 * the server does), that user's state infor should be empty
 */

#ifndef __GAME_H__
#define __GAME_H__

#include <string>

namespace dfc{

class Game{
private:

};

} // namespace dfc

#endif /*__GAME_H__*/

#! /bin/bash

scryfile=$HOME/.hellas

ENGINE="deluge"
if [ "$1x" == "-tx" ]; then
	ENGINE="transmission"
	shift
fi

echo -n "Date scried: " > $scryfile
date >> $scryfile
ssh hellas@hestia.feralhosting.com -t "find private/$ENGINE/data/ -type f" | sed "s/private\/$ENGINE\/data\///" >> $scryfile

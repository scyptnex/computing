#! /bin/sh

scryfile=$HOME/.hellas

echo -n "Date scried: " > $scryfile
date >> $scryfile
ssh hellas@hestia.feralhosting.com -t "find private/deluge/data/ -type f" | sed 's/private\/deluge\/data\///' >> $scryfile

package foo.bar.canva.util;

import foo.bar.canva.QueueSendResponse;

public class SendFull extends QueueSendResponse{
    public SendFull() {
        super("The way is shut, it was made by the dead and the dead keep it"); // irl this is a localized string from the props file
    }
}
